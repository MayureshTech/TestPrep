/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/TestPrep',
  assetPrefix: '/TestPrep',
  images: {
    unoptimized: true,
  },
}

export default nextConfig
